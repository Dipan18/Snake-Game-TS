import { Position } from './position.js';

export const UP: Position = { x: -1, y: 0 };
export const DOWN: Position = { x: 1, y: 0 };
export const LEFT: Position = { x: 0, y: -1 };
export const RIGHT: Position = { x: 0, y: 1 };
export const STLL: Position = { x: 0, y: 0 };
